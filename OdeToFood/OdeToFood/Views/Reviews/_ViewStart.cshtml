@*
    Each _ViewStart.cshtml file is applied recursively. For example, the
    _ViewStart.cshtml file in the root of the Views directory applies to
    all views. 
    
    This file in the Reviews directory "overrides" the _ViewStart.cshtml
    file in the Views directory.
*@

@{
    Layout = "~/Views/Shared/_Layout2.cshtml";
}