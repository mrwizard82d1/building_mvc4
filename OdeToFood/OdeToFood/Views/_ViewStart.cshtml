@*
    The Razor engine will start its view processing by looking for the
    file _ViewStart.cshtml.
    
    The engine then executes the code in the code block at the top of the
    file before rendering anything else.
*@

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}